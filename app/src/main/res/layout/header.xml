<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="200dp"
    android:background="#3A4866">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/astechlogo"
        android:layout_alignTop="@+id/accAvatar"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="29dp" />
    <ImageView
        android:layout_marginTop="5dp"
        android:id="@+id/accAvatar"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="10dp"
        android:src="@drawable/punda"/>
    <TextView
        android:textSize="15dp"
        android:id="@+id/acc_name_view"
        android:layout_alignLeft="@+id/accAvatar"
        android:layout_below="@+id/accAvatar"
        android:textColor="#ffffff"
        android:text="account Name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</RelativeLayout>